import XCTest
@testable import OCWaveView

final class OCWaveViewTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OCWaveView().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
